import 'package:zbflutter/Global/ZBGlobalHeader.dart';
class BDStoreWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title: Text('Store'),
      ),
      body:Center(
        child: Text('Store'),
      )
    );
  }
}