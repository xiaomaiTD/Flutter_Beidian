import 'package:zbflutter/Global/ZBGlobalHeader.dart';
class BDDiscoveryWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title: Text('Discovery'),
      ),
      body:Center(
        child: Text('Discovery'),
      )
    );
  }
}